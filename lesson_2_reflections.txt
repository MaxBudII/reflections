What happens when you initialize a repository?  Why do you need to do it?
	When init'ing a repository, git creates the .git folder so start tracking changes to file when the commit command it run

	This is needed to make the current directory a Git contolled directory.

How is the staging area different from the working directory and the repository?  What value do you this it offers?
	The Working Directory is where the current files actually exist
	The Starting Area shows what files will be included in any future commits
	The Repository is the collection of the different versions of the files

How can you use the staging area to make sure you have one commit oer logical change?
	After each change, add the changed file to git then commit

What are some situation when branched would be helpful in keeping your history organized? How would branches help?
	Branched can help separate expiremental code changes from mail code changes while still allowing for commits and code change tracking.

How do the diagrams help you visualize the branch structure?
	The diagrams show me the structure of the branches and their commit status.

What is the result of merging two branches together?  Why do we repsresent it in a diagram the way we do?
	Merging brings the changes from both banches into one branch.

What are the pros and cons of Git's automatic merging vs. always doing merges manually?
	Automatic merging saves time.  Manual merging required a lot of code review

